spring:
  application:
    name: gateway
  cloud:
    consul:
      config:
        enabled: true
        defaultContext: defaults
        prefixes: config
        format: yaml
        data-key: application.yml