spring:
  application:
    name: gateway-service
  cloud:
    consul:
      config:
        enabled: true
        defaultContext: defaults
        prefixes: config
        format: yaml
        data-key: application.yml